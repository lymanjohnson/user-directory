<!DOCTYPE html>
 <html>
   <head>
     <title></title>
     <link rel="stylesheet" type="text/css" href="/styles-user.css">
   </head>
   <body>
     <img src="{{avatar}}" alt="">
      <h1>{{name}}</h1>
      {{#job}}
      <p>{{location.city}}, {{location.country}} | {{job}}, {{company}}</p>
      {{/job}}
      {{^job}}
      <span><p>{{location.city}}, {{location.country}} | <p class ="emphasis">Available for Hire</p></span>
      {{/job}}
      <p>{{email}} | {{phone}}</p>
      {{#university}}
      <h2>Education:</h2>
      <p>{{university}}</p>
      {{/university}}
      {{#skills}}
      <h2>Skills</h2>
        <li>{{.}}</li>
      {{/skills}}
   </body>
 </html>
